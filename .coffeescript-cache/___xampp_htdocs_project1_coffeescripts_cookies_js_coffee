{"mtime":1406316180,"js":"(function() {\n  var SetCookie, delCookie, getCookie, sess;\n\n  sess = function(obj) {\n    var the_cookie;\n\n    the_cookie = obj.name;\n    if (getCookie(\"users_resolution\") == null) {\n      SetCookie(\"users_resolution\", the_cookie);\n    } else {\n      delCookie(\"users_resolution\");\n      SetCookie(\"users_resolution\", the_cookie);\n    }\n  };\n\n  SetCookie = function(name, value) {\n    var Days, exp;\n\n    Days = 1;\n    exp = new Date();\n    exp.setTime(exp.getTime() + Days * 24 * 60 * 60 * 1000);\n    document.cookie = name + \"=\" + escape(value) + \";expires=\" + exp.toGMTString();\n  };\n\n  getCookie = function(name) {\n    var arr;\n\n    arr = document.cookie.match(new RegExp(\"(^| )\" + name + \"=([^;]*)(;|$)\"));\n    if (arr != null) {\n      return unescape(arr[2]);\n    }\n    return null;\n  };\n\n  delCookie = function(name) {\n    var cval, exp;\n\n    exp = new Date();\n    exp.setTime(exp.getTime() - 1);\n    cval = getCookie(name);\n    if (cval != null) {\n      document.cookie = name + \"=\" + cval + \";expires=\" + exp.toGMTString();\n    }\n  };\n\n}).call(this);\n"}